// Falsy


// A value is considered "falsy" if it converts to false when evaluated in a boolean context. 
// JavaScript has exactly six falsy values:
// false — the boolean false
// 0 — the number zero"" or 
// '' — an empty string
// null — denotes a null value
// undefined — indicates an uninitialized variable or property
// NaN — represents a "Not-a-Number" value