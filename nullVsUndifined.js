

// undifined

// it means that ka ekk variable ko declared kia gya hu but koi value assign nh hu woh undifined ata hai  
// It is also the default return value of functions that do not explicitly return anything.
// When accessing an object property or array element that does not exist, the result is undefined.

let fruits;
// console.log(fruits);


// Null

// null is an intentional absence of any object value. It is assigned to variables to denote that it explicitly contains no meaningful value.
// It is often used where an object is expected but is not applicable or necessary.

let sum = null;

